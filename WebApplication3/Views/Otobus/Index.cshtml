@model IEnumerable<WebApplication3.Models.Otobus>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Plaka)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.rota)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OtobusKapasite)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Firma)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Plaka)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.rota)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OtobusKapasite)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Firma.FirmaID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.OtobusID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.OtobusID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.OtobusID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
